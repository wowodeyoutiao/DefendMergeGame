# 战斗数字字体

`monster-damage/` 使用原美术库 `UI切图/字体/Power/怪物掉血*.png`，与同目录的 `HP_Jian_Monster.fnt` 位图字库一致。

保留原始 PNG；数字改名为 `0.png` 至 `9.png`，正负号为 `plus.png`、`minus.png`。显示时按字符组合，不依赖操作系统字体或原资源目录。首次进入页面预加载，普通命中、地雷、主角大招和全屏卡牌均使用此套数字。
